# Build output
.pio/

# Generated MJPEG frames (regenerate via ffmpeg — see README)
data/

# Local backups of prior firmware revisions
*.bak.txt

# WiFi credentials (local only — never commit)
src/secrets.h
