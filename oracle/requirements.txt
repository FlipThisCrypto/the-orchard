# The Orchard — Oracle service runtime dependencies.
# Tested on Python 3.11+ (Richard's local install is 3.14).

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
pydantic>=2.7.0
pydantic-settings>=2.3.0
# Wallet session tokens (app/sessions.py, HS256). Was missing here and
# only worked when a globally-installed copy happened to exist — CI's
# clean-venv run caught it.
PyJWT>=2.8.0
# BLS verification of wallet-signed login messages (app/wallet_auth.py).
# Imported lazily there, but require_wallet_session=true is the
# production posture, so it's a hard runtime dep — same CI catch.
chia_rs>=0.42.0
# Schema migrations (oracle/migrations, alembic.ini). Real, versioned
# migrations for the Postgres-readiness path (T4/D4); the SQLite PoC uses
# the same baseline. For Postgres, also install a driver and point
# ORCHARD_ORACLE_DB_URL at it, e.g.:
#   pip install "psycopg[binary]>=3.1"
#   ORCHARD_ORACLE_DB_URL=postgresql+psycopg://user:pass@host/orchard
alembic>=1.13
# Orchard Pass verification (app/pass_verify.py -> orchard_chia.nft.verify ->
# orchard_chia.wallet.rpc) reaches these. They were undeclared here, so a clean
# oracle-only install crashed at startup with an ImportError. The import is now
# lazy (a missing dep degrades to a 503 on /register instead of killing the
# service), but the dependency is real whenever wallet binding is used.
# Floors match orchard_chia/requirements.txt. Note: orchard_chia itself is NOT
# a dependency of the oracle — the oracle must boot without it (713c92c).
# Known drift: oracle/requirements.lock does not yet carry requests/urllib3;
# regenerating the --require-hashes lock is maintainer work.
requests>=2.32.0
urllib3>=2.7.0

# Dev / tests (kept here for one-command install; trim to a separate
# requirements-dev.txt later if it grows).
httpx>=0.27.0          # FastAPI TestClient pulls this in but pinning explicitly
pytest>=8.0.0
