# The Orchard — Oracle service config.
#
# Copy to oracle/.env and edit. .env is in .gitignore — your local
# values stay local. Defaults below are sensible for a local single-PC
# deployment.

# Where to listen.
#
# Default 127.0.0.1 (loopback) is safe — the oracle is only reachable
# from this machine. Use this for development or when the dashboard +
# oracle + tunnel all run on the same host.
#
# Set 0.0.0.0 ONLY if you need real Trees on your LAN to POST signed
# readings directly to this host. Doing so makes /register reachable
# from every device on that network and there is currently no auth on
# /register, so anyone on the LAN can claim arbitrary node_ids.
ORCHARD_ORACLE_HOST=127.0.0.1
ORCHARD_ORACLE_PORT=8000

# Where SQLite stores readings + registrations.
# Defaults to oracle/data/orchard.db (created on first run).
ORCHARD_ORACLE_DB_URL=sqlite:///./oracle/data/orchard.db

# uvicorn log level: critical, error, warning, info, debug, trace
ORCHARD_ORACLE_LOG_LEVEL=info

# Day-aligned Season anchor (UTC midnight of this date == start of Season 1).
# v1 uses simple day-based Seasons; Phase 5 replaces this with real
# Chia-block-aligned Seasons via DataLayer.
ORCHARD_SEASON_GENESIS_DATE=2026-05-27

# Hosted claim page (GET /claim). WalletConnect project id the browser uses to
# open the wallet modal so operators can claim a Tree by proving Orchard Pass
# ownership. This is a PUBLIC client id, not a secret. Get a free one at
# https://cloud.reown.com (reuse the dashboard's ORCHARD_VIEW_WC_PROJECT_ID if
# you already have one). Leave empty to disable the claim page's Connect button.
ORCHARD_ORACLE_WC_PROJECT_ID=
