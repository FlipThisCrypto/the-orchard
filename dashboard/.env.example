# The Orchard — Orchard View (local dashboard) config.
#
# Copy to dashboard/.env and edit. .env is gitignored.

# Where Orchard View itself listens.
ORCHARD_VIEW_HOST=127.0.0.1
ORCHARD_VIEW_PORT=5000

# Where the oracle is running. Default assumes localhost:8000 (same PC).
ORCHARD_VIEW_ORACLE_URL=http://127.0.0.1:8000

# Default oracle URL the dashboard will push to a freshly-provisioned Tree.
# Usually your dev PC's LAN IP + the oracle port. If you want real Trees
# on the LAN to reach the oracle directly, set ORCHARD_ORACLE_HOST=0.0.0.0
# in oracle/.env and use that PC's LAN IP here. Placeholder below.
ORCHARD_VIEW_TREE_ORACLE_URL=http://192.168.1.10:8000/readings

# Serial timeout in seconds for Tree commands. 3s is comfortable for
# the Tree's line-by-line responses.
ORCHARD_VIEW_SERIAL_TIMEOUT=3.0

# Public-demo mode. Set to 1/true when serving the dashboard to the
# internet (e.g. via Cloudflare Tunnel). Hides "Plant a Tree" and
# returns 404 for /provision, /api/serial/*, and /api/oracle/register.
# Read-only endpoints (Trees list, live readings) remain available.
ORCHARD_VIEW_PUBLIC_MODE=0

# Phase 6.6 — WalletConnect project ID. Free from
# https://cloud.walletconnect.com. Needed for the Connect Wallet
# button to talk to Sage / Goby. Leave empty to fall back to a
# disabled-but-friendly state.
ORCHARD_VIEW_WC_PROJECT_ID=
