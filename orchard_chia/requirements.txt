# The Orchard — Chia integration runtime dependencies.
# Used by chia.datalayer (Phase 5) and chia.payout (Phase 7).

requests>=2.32.0
# urllib3 is imported directly in orchard_chia/wallet/rpc.py and
# orchard_chia/datalayer/rpc.py (we call urllib3.disable_warnings).
# Pin explicitly to 2.7.0+ which fixes CVE-2024-37891 / CVE-2025-50181
# / CVE-2026-44431 and friends — requests does not floor it high
# enough.
urllib3>=2.7.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0
# ADR-0007 device/season signatures (datalayer/schema.py): `cryptography`
# verifies secp256r1 ECDSA; `ecdsa` provides RFC 6979 *deterministic*
# signing (cryptography has no RFC 6979 API). Signing happens only on
# our own boxes with our own keys, so pure-python `ecdsa` is acceptable.
cryptography>=42.0.0
ecdsa>=0.19.0
# Chia consensus VM (clvm_rs bindings) — the ADR-0007 cross-check runs
# `secp256r1_verify` against our signatures (tests/test_clvm_secp.py),
# and the future orchard-verify on-chain mode will need it anyway.
chia_rs>=0.42.0

# Dev / tests.
pytest>=8.0.0
