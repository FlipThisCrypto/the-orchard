# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
env/
.env
.env.*
!.env.example

# --- PlatformIO / Arduino / ESP32 ---
firmware/.pio/
firmware/.vscode/
firmware/.pioenvs/
firmware/.piolibdeps/
firmware/lib/readme.txt
firmware/build/
firmware/CMakeListsPrivate.txt
firmware/CMakeCache.txt
firmware/CMakeFiles/
*.elf
*.bin
*.hex

# Exception: the merged web-flasher firmware images ARE committed —
# they're the production artifact a browser flasher needs to serve.
# Bins under flasher/ are reproducible from firmware/ source via
# `esptool merge-bin` (see flasher/README.md).
!flasher/**/*.bin

# --- Node (future dashboard frontend bits) ---
node_modules/
npm-debug.log
yarn-error.log
.pnpm-store/

# --- IDE / Editors ---
.idea/
.vscode/*
!.vscode/settings.example.json
!.vscode/extensions.json
*.swp
*.swo
*~

# --- OS junk ---
.DS_Store
Thumbs.db
desktop.ini

# --- Secrets / credentials (NEVER commit these) ---
*.pem
*.key
*.cert
*.crt
*_secret*
secrets/
config/local.*
chia_keys.txt
mnemonic.txt
wallet_fingerprint.txt

# Token creation log (contains operator-private wallet fingerprint + wallet id)
docs/Juice Token.docx
docs/*token*.docx
docs/token/*-private.*

# Driver installers operators may download into the tree (Windows USB-UART drivers).
# Not source — don't commit. Common naming patterns:
**/CH34*SER.EXE
**/CH34*SER.exe
**/CP210x*Setup*.exe
**/CP210x*Setup*.EXE
**/FT_Prog*.exe

# --- Runtime data ---
*.db
*.sqlite
*.sqlite3
data/
logs/
*.log

# --- Component-local configs (operator-private, gitignored) ---
orchard_chia/config.yaml
orchard_chia/data/
oracle/.env
dashboard/.env

# Operator's local mint plan (has addresses + final URIs). The
# .example.yaml template stays in git for other contributors.
nft/mint_plan.yaml
nft/mint_results.json

# Source NFT media — the .mp4 originals live on IPFS via Filebase
# (defiant-black-skink.myfilebase.com); the repo only ships the
# generated metadata JSONs that point at the IPFS CIDs.
nft/metadata/*.mp4

# mintgarden-studio export folder (operator-side bookkeeping; the
# canonical record is the on-chain NFTs themselves).
nft/metadata/mintgarden-results/

# --- Claude / dev tooling local state ---
.claude/

orchard_chia/data/
orchard_chia/**/ops/*.jsonl
orchard_chia/**/ops/*.jsonl.1

